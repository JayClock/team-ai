<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="reengineering.ddd.teamai.mybatis.mappers.UserProjectsMapper">

    <resultMap id="project" type="reengineering.ddd.teamai.model.Project">
      <id property="identity" column="id" jdbcType="VARCHAR" javaType="String"/>
      <association property="description" javaType="reengineering.ddd.teamai.description.ProjectDescription">
        <constructor>
          <arg column="name" jdbcType="VARCHAR" javaType="String"/>
        </constructor>
      </association>
      <association property="members" javaType="reengineering.ddd.teamai.mybatis.associations.ProjectMembers">
        <result column="id" property="projectId" javaType="int"/>
      </association>
      <association property="conversations" javaType="reengineering.ddd.teamai.mybatis.associations.ProjectConversations">
        <result column="id" property="projectId" javaType="int"/>
      </association>
      <association property="logicalEntities" javaType="reengineering.ddd.teamai.mybatis.associations.ProjectLogicalEntities">
        <result column="id" property="projectId" javaType="int"/>
      </association>
      <association property="diagrams" javaType="reengineering.ddd.teamai.mybatis.associations.ProjectDiagrams">
        <result column="id" property="projectId" javaType="int"/>
      </association>
    </resultMap>

  <select id="findProjectByUserAndId" resultMap="project">
    SELECT p.id, p.name
    FROM projects p
    WHERE p.id = #{id}
    AND EXISTS (
      SELECT 1 FROM project_members pm
      WHERE pm.project_id = p.id AND pm.user_id = #{user_id}
    )
  </select>

  <insert id="insertProject" useGeneratedKeys="true" keyProperty="holder.id" keyColumn="id"
          parameterType="reengineering.ddd.mybatis.support.IdHolder">
    INSERT INTO projects(name, creator_id)
    VALUES (#{description.name}, #{user_id})
  </insert>

  <insert id="addMember">
    INSERT INTO project_members(project_id, user_id, role)
    VALUES (#{project_id}, #{user_id}, #{role})
  </insert>

  <select id="countProjectsByUser" resultType="int">
    SELECT COUNT(*) FROM project_members WHERE user_id = #{user_id}
  </select>

  <select id="findProjectsByUserId" resultMap="project">
    SELECT p.id, p.name
    FROM projects p
    INNER JOIN project_members pm ON p.id = pm.project_id
    WHERE pm.user_id = #{user_id}
    ORDER BY pm.joined_at DESC
    LIMIT #{size} OFFSET #{from}
  </select>
 </mapper>
