<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.businessdrivenai.persistence.mybatis.mappers.UsersMapper">
  <resultMap id="user" type="com.businessdrivenai.domain.model.User">
    <id property="identity" column="id" jdbcType="BIGINT" javaType="String"/>
    <association property="description" javaType="com.businessdrivenai.domain.description.UserDescription">
      <constructor>
        <arg column="name" jdbcType="VARCHAR" javaType="String"/>
        <arg column="email" jdbcType="VARCHAR" javaType="String"/>
      </constructor>
    </association>
    <association property="accounts" javaType="com.businessdrivenai.persistence.mybatis.associations.UserAccounts">
      <result column="id" property="userId" javaType="int"/>
      <collection property="list" resultMap="com.businessdrivenai.persistence.mybatis.mappers.UserAccountsMapper.account"
                  ofType="com.businessdrivenai.domain.model.Account"/>
    </association>
    <association property="projects" javaType="com.businessdrivenai.persistence.mybatis.associations.UserProjects">
      <result column="id" property="userId" javaType="int"/>
    </association>
  </resultMap>

  <select id="findUserById" resultMap="user">
    select u.id,
           u.name,
           u.email,
           a.id as account_id,
           a.provider,
           a.provider_id,
           p.id as project_id,
           p.name
    from users u
           left join accounts a on u.id = a.user_id
           left join project_members pm on u.id = pm.user_id
           left join projects p on pm.project_id = p.id
    where u.id = #{id}
  </select>

  <insert id="insertUser" useGeneratedKeys="true" keyProperty="holder.id" keyColumn="id"
          parameterType="com.businessdrivenai.persistence.support.IdHolder">
    insert into users (name, email)
    values (#{description.name}, #{description.email})
    on conflict (email) do update set name = excluded.name
    returning id
  </insert>

  <update id="updateUser">
    update users
    set name  = #{request.name},
        email = #{request.email}
    where id = #{id}
  </update>
</mapper>
